{
  "name": "crenit-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:app": "npm --prefix frontend run dev",
    "dev:all": "concurrently \"npm:dev:backend\" \"npm:dev:app\"",
    "build:tenant": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
