/** Inline before paint to avoid light flash when dark mode is saved. */
export const THEME_INIT_SCRIPT = `(function(){try{var t=localStorage.getItem('crenit-theme');if(t!=='dark')return;document.documentElement.classList.add('dark');var p=localStorage.getItem('crenit-dark-palette');if(!p)return;var o=JSON.parse(p);var r=document.documentElement;Object.keys(o).forEach(function(k){var m={'bg':'--rc-bg','card':'--rc-card','cardAlt':'--rc-card-alt','elevated':'--rc-elevated','border':'--rc-border','text':'--rc-text','textSecondary':'--rc-text-secondary','textMuted':'--rc-text-muted','accentSurface':'--rc-accent-surface','hover':'--rc-hover'};if(m[k])r.style.setProperty(m[k],o[k]);});}catch(e){}})();`;
